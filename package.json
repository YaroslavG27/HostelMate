{
  "name": "hostelmate",
  "version": "1.0.0",
  "type": "module",
  "description": "1. Create .env Copy `.env.example` file, name it `.env` and add credentials. 2. To run this project, run the following command in the terminal: npm run dev",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.1.4"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.3.2",
    "prettier-eslint": "^16.3.0"
  }
}
